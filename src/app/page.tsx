import { BookNow, Header, NewsletterBanner, Services } from "@/containers";

const Home = () => (
  <>
    <Header />
    <BookNow />
    <NewsletterBanner />
    <Services />
  </>
);

export default Home;
